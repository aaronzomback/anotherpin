<h2>Thank you!</h2>
<h3>Your order has been received</h3>

<%= link_to "Back to home", root_path %>



<p>
  <%= @order.first_name %>
  <%= @order.city %>

  <% @order.order_items.each do |item| %>
    <%= item.product.title %>

  <% end %>
</p>
